services:
  kumorfm:
    build: .
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./secrets:/app/secrets
      - ./drive_cache:/app/drive_cache
      - ./datasets:/app/datasets
    command: ["python", "run_pipeline.py"]
